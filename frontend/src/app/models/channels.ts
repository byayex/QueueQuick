export interface channels_enabled {
    email: string
}

export interface email {
    host: string;
    port: number;
    username: string;
    password: string
    sender_address: string;
    sender_name: string;
}